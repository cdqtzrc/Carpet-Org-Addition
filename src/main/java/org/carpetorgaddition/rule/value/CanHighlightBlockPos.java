package org.carpetorgaddition.rule.value;

public enum CanHighlightBlockPos {
    /**
     * 不显示高亮路径点按钮
     */
    FALSE,
    /**
     * 使用{@code  OMMC}的高亮路径点
     */
    OMMC,
    /**
     * 使用{@code Carpet Org Addition}的高亮路径点
     */
    DEFAULT
}
